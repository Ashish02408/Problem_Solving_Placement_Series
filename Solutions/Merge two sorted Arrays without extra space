class Solution:
    def merge(self, nums1: List[int], m: int, nums2: List[int], n: int) -> None:
        x = len(nums1)-1
        if n==0:
            return
        for i in range(n):
            nums1[x-i]=nums2[i]
        return nums1.sort()
            
       
